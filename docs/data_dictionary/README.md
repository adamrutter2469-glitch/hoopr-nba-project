# Data dictionary

Auto-generated by [`scripts/build_data_dictionary.R`](../../scripts/build_data_dictionary.R)
from the pipeline's **live parquet schemas**, not hand-typed - so it can't silently drift out
of sync with the real data the way a static document would. Re-run that script any time a
table's columns change:

```bash
Rscript scripts/build_data_dictionary.R
```

## Format

One CSV per table. Row 1 describes the table itself; every row after that describes one
column, in the table's actual column order.

| Column | Table row (row 1) | Column rows (row 2+) |
|---|---|---|
| `row_num` | `1` | position in the table, `2..n+1` |
| `name` | table name | column name |
| `description` | what the table is, high level | what the column holds |
| `logic` | `NA` | how it's derived - `NA` if pulled with no transformation |
| `sources` | core source tables | `;`-separated source column(s), or the raw API call |
| `dtype` | `NA` | column's actual type, read from the live data |
| `feature_family` | `NA` | tag for filtering - see below |
| `leakage_risk` | `NA` | whether it's safe to use predicting *that same game* - see below |

## `feature_family` values

`identifier`, `metadata`, `context` (rest/travel/home-away), `raw_actual` (this exact game's
actual result), `own_rolling`, `opponent_rolling`, `allowed`, `opponent_allowed`, `matchup`,
`reference` (player/team master data), `unclassified` (not recognized - filled in with a
`NEEDS REVIEW` placeholder instead of a guess; grep for it before trusting a table fully).

## `leakage_risk` values

This is the same distinction that `models/train_rebounds_model.R`'s predictor allow-list is
built around: `safe` columns are fixed before tip-off; `UNSAFE` columns are that exact game's
actual outcome and must never be used to predict that same game (they're fine as a label/
target, or for post-game analysis). `NA` means unclassified - check the column yourself.

## Tables

| Table | Rows x cols | Grain |
|---|---|---|
| [`team_game_features`](team_game_features.csv) | 9,840 x 278 | 1 row/team-game - **the main table.** Box score, rest/travel, rolling averages, opponent/matchup features. |
| [`player_game_features`](player_game_features.csv) | 105,253 x 92 | 1 row/player-game. |
| [`team_rebounding_features`](team_rebounding_features.csv) | 9,840 x 14 | 1 row/team-game - parsed advanced rebounding splits, this-game actuals only. |
| [`player_rebounding_features`](player_rebounding_features.csv) | 22,818 x 18 | 1 row/player-game - player-grain analog, scoped by `cfg$player_rebounding_seasons`/`_min_minutes`. |
| [`play_by_play`](play_by_play.csv) | 2,501,259 x 66 | 1 row/play - event-level, ESPN-sourced (`hoopR::load_nba_pbp()`); `game_id_nba` bridged in from the schedule. |
| [`team_id_mapping`](team_id_mapping.csv) | 30 x 5 | 1 row/team - bridge to Big Balls Sports Data's team ids. |
| [`player_id_mapping`](player_id_mapping.csv) | 4,731 x 5 | 1 row/player - bridge to Big Balls Sports Data's player ids. |
| [`espn_player_id_mapping`](espn_player_id_mapping.csv) | 872 x 4 | 1 row/player - bridge to ESPN's athlete ids (play_by_play). |
| [`player_turnover_features`](player_turnover_features.csv) | 105,253 x 27 | 1 row/player-game - turnover detail mined from play_by_play (bad pass, lost ball, offensive foul, traveling, ...), plus steals forced. 93.00% exact match to player_game_logs.tov (gap is a real ESPN data limit, not a bug - see table description). |
| [`player_block_features`](player_block_features.csv) | 105,253 x 14 | 1 row/player-game - block detail mined from play_by_play, by shot type (dunk/layup/hook/tip/jump_shot). 99.68% exact match to player_game_logs.blk. Also includes fg_blocked_* - how often a player's own shot gets blocked, a stat the box score doesn't track at all. |
| [`player_foul_features`](player_foul_features.csv) | 105,253 x 28 | 1 row/player-game - foul detail mined from play_by_play: by subtype (pf_shooting, pf_personal, ...), by period (pf_q1..pf_q4/pf_ot), WHEN each foul-trouble threshold hit on the raw game clock (elapsed_seconds_at_pf_2/3/6), and the same milestones re-anchored to the player's own on-court time (checkin_elapsed_seconds, onct_elapsed_at_pf_2/3/6) - timing the box score can't provide at all. 96.31% exact match to player_game_logs.pf (eligible foul types chosen empirically, not assumed). |
| [`player_shot_zone_features`](player_shot_zone_features.csv) | 105,253 x 21 | 1 row/player-game - shot location detail mined from play_by_play coordinates: FGA/FGM per court zone (restricted area, paint non-RA, left/center/right mid-range, left/right corner 3, above-the-break 3). 2PT/3PT boundary verified against real recorded shot values (99.966% match); zone totals validated against player_game_logs.fga/fgm (98.33%/99.73%). |
| [`player_archetypes`](player_archetypes.csv) | 1,613 x 21 | 1 row/player-season - combined offense+defense play-style archetype (k=9) + tier (caliber within archetype). Box-score shape features only. |
| [`player_offensive_archetypes`](player_offensive_archetypes.csv) | 1,606 x 23 | 1 row/player-season - offense-ONLY play-style archetype (k=9), enriched with real shot-zone data and alley-oop rate. No tier layer yet. |
| [`player_defensive_archetypes`](player_defensive_archetypes.csv) | 406 x 20 | 1 row/player-season, **2025-26 ONLY** (scoped by player_rebounding_features' current season coverage) - defense/hustle-ONLY play-style archetype (k=9): steal type, block type, rebounding (combined off+def, contested share, distance zone), and foul rate/on-court-time-relative early-foul-trouble rate. No offensive stats at all. No tier layer yet. |
| [`schedule_with_travel_detail`](schedule_with_travel_detail.csv) | 5,303 x 18 | 1 row/game. |
| [`schedule_team_level_final`](schedule_team_level_final.csv) | 10,606 x 16 | 1 row/team-game. |
| [`schedule`](schedule.csv) | 5,303 x 10 | 1 row/game - raw pull. |
| [`team_game_logs`](team_game_logs.csv) | 9,840 x 30 | 1 row/team-game - raw pull. |
| [`player_game_logs`](player_game_logs.csv) | 105,253 x 33 | 1 row/player-game - raw pull. |
| [`players_raw`](players_raw.csv) | 5,205 x 42 | 1 row/player - reference data. Several `.x`/`.y`-suffixed columns from hoopR's own internal join are genuinely ambiguous and flagged `NEEDS REVIEW` rather than guessed at. |
| [`teams_raw`](teams_raw.csv) | 30 x 36 | 1 row/team - reference data. |

Row/col counts as of the run that generated these files - re-run the script and they'll
update automatically as the pipeline pulls more games. This table listing itself is
hand-maintained (not auto-generated like the CSVs) - update it here when a table is
added/removed.
